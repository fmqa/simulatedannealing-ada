library project Bitmaps is

   for Languages use ("Ada");
   for Library_Name use "Bitmap";
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Library_Dir use "lib";

   package Compiler is
      for Switches ("ada") use ("-O3", "-gnatp", "-flto");
   end Compiler;

end Bitmaps;

